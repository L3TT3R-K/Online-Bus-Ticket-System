document.addEventListener("DOMContentLoaded",function(){const box=document.getElementById("ticketList");const tickets=JSON.parse(localStorage.getItem("tickets")||"[]");if(tickets.length===0){box.innerHTML=`<div class="alert alert-info">Bạn chưa có vé nào. Hãy tìm chuyến và đặt vé thử.</div>`;return}box.innerHTML=tickets.reverse().map(ticket=>`<div class="ticket-card ticket-item mb-3"><div class="row align-items-center g-3"><div class="col-md-8"><h5 class="fw-bold">${ticket.from} → ${ticket.to}</h5><p class="mb-1"><strong>Mã vé:</strong> ${ticket.id}</p><p class="mb-1"><strong>Nhà xe:</strong> ${ticket.company}</p><p class="mb-1"><strong>Ngày giờ:</strong> ${ticket.date} - ${ticket.time}</p><p class="mb-1"><strong>Ghế:</strong> ${ticket.seats.join(", ")}</p><p class="mb-1"><strong>Hành khách:</strong> ${ticket.name} - ${ticket.phone}</p></div><div class="col-md-4 text-md-end"><div class="trip-price mb-2">${money(ticket.total)}</div><span class="badge bg-success">${ticket.status}</span><div class="mt-3"><button class="btn btn-outline-primary btn-sm" onclick="window.print()"><i class="fa-solid fa-print"></i> In vé</button></div></div></div></div>`).join("")});