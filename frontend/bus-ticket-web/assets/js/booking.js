document.addEventListener("DOMContentLoaded",function(){const tripId=getParam("tripId")||1;const seats=(getParam("seats")||"").split(",").filter(Boolean);const trip=getTripById(tripId);if(!trip||seats.length===0){document.getElementById("bookingContent").innerHTML=`<div class="alert alert-danger">Thông tin đặt vé không hợp lệ.</div>`;return}document.getElementById("bookingTrip").innerHTML=`<h4 class="fw-bold">${trip.company}</h4><p>${trip.from} → ${trip.to}</p><p><strong>Ngày đi:</strong> ${trip.date}</p><p><strong>Giờ đi:</strong> ${trip.time}</p><p><strong>Điểm đón:</strong> ${trip.startStation}</p><p><strong>Điểm trả:</strong> ${trip.endStation}</p><p><strong>Ghế:</strong> <span class="fw-bold text-primary">${seats.join(", ")}</span></p><p><strong>Tổng tiền:</strong> <span class="fw-bold text-danger">${money(seats.length*trip.price)}</span></p>`;const form=document.getElementById("bookingForm");form.addEventListener("submit",function(event){event.preventDefault();const customerName=document.getElementById("customerName").value.trim();const phone=document.getElementById("phone").value.trim();if(!customerName||!phone){alert("Vui lòng nhập đầy đủ thông tin hành khách.");return}const query=new URLSearchParams({tripId:trip.id,seats:seats.join(","),name:customerName,phone:phone});window.location.href="payment.html?"+query.toString()})});